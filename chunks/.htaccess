<FilesMatch "\.gz$">
	Header append Content-Encoding "gzip"
	Header append Cache-Control "no-cache"
	# this doesn't work until 2.2.12 (we're on 2.2.11)
	# Header set Content-Type "application/json"
</FilesMatch>
